<?xml version="1.0" encoding="utf-8"?>
<!--
  VINTF manifest for the OpenAgent e1 AI SoC.

  This manifest is intentionally scaffold-only. The hardware register layout is
  defined in sw/platform/e1_platform_contract.json.

  HALs deliberately absent (no implementation, no backing Linux node):
    camera, audio, radio, GNSS, NFC, bluetooth, wifi, sensors, vibrator,
    fingerprint, thermal, power, health, USB, keymaster/gatekeeper/weaver,
    drm.

  Reserved future HAL package/service markers:
    e1_npu.default
    hwcomposer.openagent_ai_soc

  Do not add active <hal> entries until the matching source or reviewed
  prebuilts are imported into the external AOSP tree and the VINTF/vendorimage
  evidence logs are captured. The checked-in HAL stubs are fail-closed source
  scaffolds only; this file must not claim they are active vendor services.
-->
<manifest version="1.0" type="device">

</manifest>
