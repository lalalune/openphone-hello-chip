# OpenSBI port placeholder

The full SoC target should boot through OpenSBI before U-Boot and Linux.

The hello chip has no CPU, so OpenSBI integration starts once the Chipyard/Rocket subsystem exists.
