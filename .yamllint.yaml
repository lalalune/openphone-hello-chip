extends: default

ignore: |
  .venv/
  build/
  external/
  tools/
  .claude/

rules:
  document-start: disable
  line-length: disable
  truthy:
    allowed-values: ["true", "false", "on", "off"]
  comments:
    min-spaces-from-content: 1
